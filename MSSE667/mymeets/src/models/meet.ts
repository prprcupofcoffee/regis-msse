import { CourseType } from '../app/course-type';

export class Meet {
    constructor(
        name: string,
        date: Date,
        location: string,
        courseType: CourseType
    ) {
        
    }
}
