// Defines the basic event types that can be swum
// at a meet. Each event type can have several
// different lengths as well; this information is
// captured elsewhere.
//
export enum EventType {
    Backstroke,
    Breaststroke,
    Butterfly,
    Freestyle,
    IndividualMedley,
    Relay
}
